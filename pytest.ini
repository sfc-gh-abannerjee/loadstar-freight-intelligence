[pytest]
markers =
    sql: tests requiring live Snowflake SQL execution
    streamlit: tests for Streamlit app validation
    notebook: tests for Jupyter notebook validation
    python: tests for Python script validation
    live: tests requiring active Snowflake connection

addopts = -v --tb=short
testpaths = tests
